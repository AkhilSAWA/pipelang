// Second example exercising sort + aggregate.
pipeline SalesReport {
    input   sales
    clean   sales
    filter  sales where amount >= 1000
    transform sales normalize_currency
    sort    sales by amount
    group   sales by region
    aggregate sales sum_amount
    output  sales
}
