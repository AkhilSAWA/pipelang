# Includes sorting and aggregation
pipeline SalesReport {
    input sales
    clean sales
    filter sales where revenue >= 1000.50
    sort sales by revenue descending
    group sales by region
    aggregate sales by revenue sum
    output sales
}